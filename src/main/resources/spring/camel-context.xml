<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://camel.apache.org/schema/spring       http://camel.apache.org/schema/spring/camel-spring.xsd">

    <!-- You could also define a traditional camel context here -->
    <!--
    <camelContext xmlns="http://camel.apache.org/schema/spring">
    <route id="proxy">
        <from uri="cxf:http://localhost:9091/greeterProxy?serviceClass=com.fusesource.examples.greeter.Greeter"/>
        <log message="SOAP proxy received : ${body}"/>
        <to uri="cxf:http://localhost:9090/greeter?serviceClass=com.fusesource.examples.greeter.Greeter"/>
        <log message="SOAP proxy response : ${body}"/>
    </route>
</camelContext>
    -->
</beans>
